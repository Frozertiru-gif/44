# Telegram Service Bot

Базовый сервис Telegram-бота для ручного создания заказов (tickets) с ролями и БД.

## Требования

- Python 3.11+
- Postgres (рекомендуется)

## Настройка окружения

1. Скопируйте `.env.example` в `.env` и заполните значения.
2. Укажите `SYS_ADMIN_IDS` через запятую (tg_id администраторов), чтобы они получили роль SYS_ADMIN после первого `/start`.

## Установка зависимостей

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## Миграции

```bash
alembic upgrade head
```

## Запуск

```bash
python -m app.main
```

## Основные команды бота

- `/start` — регистрация/обновление профиля и главное меню.
- `➕ Создать заказ` — мастер создания заявки.
- `📋 Список заказов` — фильтры и просмотр карточек.
- `👥 Пользователи` — управление ролями (только SYS_ADMIN/SUPER_ADMIN).

## Заметки

- Все ID/секреты берутся из `.env`.
- Бот публикует новые заказы в `REQUESTS_CHAT_ID`.
